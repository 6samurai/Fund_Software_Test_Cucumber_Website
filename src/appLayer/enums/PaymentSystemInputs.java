package appLayer.enums;

//enum list of all of the inputs o
public enum PaymentSystemInputs {
    NAME,
    ADDRESS,
    CARD_TYPE,
    CARD_NUMBER,
    EXPIRY_DATE,
    CVV_CODE,
    AMOUNT
}
